// import api from "./axios";

// // history is whatever the backend returned last time — pass it straight back so
// // the server stays stateless. lat/lng are only used if the user is filing a complaint.
// export async function sendChatMessage({ message, history, lat, lng }) {
//   const { data } = await api.post("/chatbot/message", { message, history, lat, lng });
//   return data; // { reply, history }
// }
